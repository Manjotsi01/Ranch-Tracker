
# Server
NODE_ENV=production
PORT=5000
BASE_API=/api

# MongoDB — rotate your password immediately, it was exposed
MONGO_URI=mongodb+srv://Ranch-Tracker:Nandha%401838%400622@cluster0.nkyw97r.mongodb.net/ranch-tracker?retryWrites=true&w=majority

# JWT — generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=replace_with_64_char_hex_string
JWT_EXPIRES_IN=7d

# CORS
CLIENT_ORIGIN=https://ranch-tracker.vercel.app

# Business config
MILK_PRICE_PER_LITRE=40
CACHE_TTL_MS=60000